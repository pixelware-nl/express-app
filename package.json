{
  "name": "express-app",
  "version": "1.0.0",
  "description": "A basic API environment to test production level VPS, Docker and Nginx setup",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
